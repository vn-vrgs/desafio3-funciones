
let a = 22;
let b = 33;

// Arrow function de una sola línea (con return implícito)
let suma = (a, b) => a + b;

// Invocando a la función y...
// Enviando por consola
console.log("El resultado es: " + suma(a, b)); // 55
// Enviando por navegador
alert("El resultado es: " + suma(a, b)); // 55

